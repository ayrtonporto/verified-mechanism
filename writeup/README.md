# Write-up source

`../writeup.pdf` is the graded artifact. To rebuild it from source:

```bash
tectonic -X compile main.tex
```

`main.tex` includes the chapters under `sections/`. Body is 10 pages; the
appendix (from page 11) is unlimited per the task instructions.
